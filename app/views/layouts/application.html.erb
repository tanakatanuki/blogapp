<!DOCTYPE html>
<html>
  <head>
    <title>Bblogapp</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    
    <header>
      <nav class="navbar navbar-inverse2">
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#gnavi">
              <span class="sr-only">メニュー</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <img class="navbar-brand2" src="/assets/bblog_head_foot.jpg">
          </div>

          <div id="gnavi" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
              <li><a href="/bblogs/home/index">TopPage</a></li>
              <li><a href="/bblogs">投稿記事</a></li>
      
              <% if logged_in? %>
                <li><%= link_to "ユーザー情報", user_path(current_user.id) %></li>
                <li><%= link_to "ログアウト", session_path(current_user.id),
                  method: :delete %></li>
              <% else %>
                <li><%= link_to "新規登録", new_user_path %></li>
                <li><%= link_to "ログイン", new_session_path %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    
    <!--
    フラッシュメッセージの表示
    -->
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class:"alert alert-#{key}") %>
    <% end %>
    
    <%= yield %>
    
    <footer>
      <div class="footer-div">
      <ul>
        <li><img src="/assets/bblog_head_foot.jpg"></li>
        <li>10月期tanaka</li>
        <li>(c)tanaka</li>
      </ul>
      </div>
    </footer>
    
  </body>
</html>
